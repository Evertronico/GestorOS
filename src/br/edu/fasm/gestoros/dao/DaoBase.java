package br.edu.fasm.gestoros.dao;

/**
 * Implementação em memória, comum a qualquer Dao<T>.
 *
 * Objetivo da Aula 05: implementar aqui o CRUD genérico (salvar,
 * buscarPorId, listarTodos, remover) sobre uma lista em memória, deixando
 * como métodos abstratos apenas o que cada subclasse concreta sabe fazer
 * de específico - ler e atribuir o identificador do seu próprio T.
 */
public abstract class DaoBase<T> implements Dao<T> {

    // TODO: implementar salvar, buscarPorId, listarTodos e remover.

    // TODO: declarar os métodos abstratos que cada subclasse deve resolver.
}
